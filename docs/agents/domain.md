# Domain Docs

This repository uses a single-context layout:

- `CONTEXT.md` contains the domain glossary.
- `docs/adr/` contains architectural decision records.

Read relevant domain docs before exploring or changing an area. Use the glossary's canonical vocabulary and explicitly flag conflicts with existing ADRs. Missing files are created lazily by the domain-modeling workflow when terms or decisions are resolved.
